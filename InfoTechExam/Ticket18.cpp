	int i; 
	switch (i) {
	case 3: i = 10; 
		break;
	case i < 0: i = ;  // i have to be constant
		break;
	}

	// 5

	union u {
		int * dim1;
		int **dim2;
	};

	for (int i = 0; i < size; i++)
	{
		if (dim1[i] < x && dim1[i] > y)
			bloodyVeryProperTargetWhichIamLookingForBuddyThough++;
			
	}