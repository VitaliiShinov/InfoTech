int i = 10;
for (int i = 0; i < 100; ++i) M[i] = N[i];
// i = 0;

// 5 

char * str;
char * substr;
int startpos;
int length;
int j = 0;

for (int = 0; i < strlen(str); i++)
{
	if (startpos >= startpos)
	{
		if (length > 0) {

			substr[j++];
			length--;
		}
	}
}