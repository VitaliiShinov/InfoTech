#include <string>

void ticket6()
{
	// 4
	// добавить точку с запятой, переместить знак поинтера на четвертой строке
	int **pc;
	int f = 0, t = 0;
	int i, j = 0;
	pc = new  int * [f];
	for (i = 0; i < t; ++i)
		pc[i] = new int[f];


	// 5
	// wat da hell?
		
	std::string res[100];
	int x = 0;

	std::string extention;
	std::string list[100];
	for(int i = 0; i<100;i++)
		if(list[i].substr(list[i].find("."),  list[i].length()) == extention)
			res[x++] = list[i];
 
}